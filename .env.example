# Remote Verification API (HTTPS fetch fallback when local did:web is unavailable)
VERIFICATION_API_URL=https://api.example.com

# Optional in-memory cache settings (SDK defaults: TTL 3600000ms / 1h, max 1000 entries)
# CACHE_TTL_MS=3600000
# CACHE_MAX_ENTRIES=1000
